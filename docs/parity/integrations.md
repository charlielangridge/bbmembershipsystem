# Legacy Integration Parity Ledger

Status: candidate-source inventory complete; production use, contracts, credentials, and owners unconfirmed
Evidence date: 18 August 2026
Evidence revision: legacy `master` at `d686bf6`

## Purpose

This ledger inventories external services and machine-to-machine contracts visible in the candidate legacy source. Every business outcome linked to a required feature remains required; obsolete SDKs, embedded assets, hard-coded endpoints, and unsafe authentication are replacement targets rather than parity requirements. `NEW-001`, provider dashboards, deployed-device samples, and production-safe configuration evidence must confirm actual use.

Configuration names below identify required secret/configuration categories only. No values are recorded.

## Payments and finance

| ID | Capability / feature | Candidate-source contract | Configuration names | Approved direction / priority | Production evidence and decisions required | Source |
| --- | --- | --- | --- | --- | --- | --- |
| INT-01 | GoCardless mandates, payments, subscriptions, and webhooks (`PAY-01`) | Pro SDK redirect flows; mandate-linked GBP subscriptions/payments in pence; `POST /gocardless/webhook`; HMAC-SHA256 `Webhook-Signature`; payment/mandate/subscription events update payments, charges, and membership | `GOCARDLESS_APP_ID`, `GOCARDLESS_APP_SECRET`, `GOCARDLESS_MERCHANT_ID`, `GOCARDLESS_ACCESS_TOKEN`, `NEW_GOCARDLESS_ACCESS_TOKEN`, `NEW_GOCARDLESS_ENV`, `NEW_GOCARDLESS_WEBHOOK_SECRET` | Current supported API/SDK; P0 | Active account/environment/API generation, webhook schema/URL, mandates/subscriptions, sandbox access, idempotency/replay/order policy, reconciliation totals, secret rotation, Finance/Technical owners | `composer.lock`; `GoCardlessHelper`; `GoCardlessPaymentController`; `GoCardlessWebhookController`; `MemberSubscriptionCharges`; webhook parser/tests |
| INT-02 | Historical direct-debit migration (`PAY-02`) | Member-triggered migration route moves legacy GoCardless arrangements toward variable payments | GoCardless names in INT-01 | Required final P3 tranche | Population still needing migration, identifiers and reconciliation, supported replacement workflow, acceptance samples | `PaymentController::migrateDD()`; `account.payment.gocardless-migrate` route/views |
| INT-03 | Stripe card payments (`PAY-03`) | Legacy Checkout.js obtains `stripeToken`; server calls `Stripe_Charge::create` in GBP and records immediate paid status; no webhook is visible | `STRIPE_API_KEY`, `STRIPE_API_PUBLIC_KEY` | Supported current Stripe flow; P0 | Account/API version, Checkout versus Payment Intents, SCA, webhook/signing secret, idempotency, refunds/disputes, fee policy, minimum-card amount, reconciliation, Finance/Technical owners | `composer.lock`; `StripePaymentController`; `StripeServiceProvider`; `StripePayment.js`; `PaymentModule.js` |
| INT-04 | PayPal subscriptions, IPN, and donations (`PAY-04`) | Hosted subscription form; unauthenticated `POST /paypal-ipn`; SDK validation; handles completed subscription payments/cancellation and unmatched donations by payer email/transaction ID | `PAYPAL_USERNAME`, `PAYPAL_PASSWORD`, `PAYPAL_SIGNATURE` | Supported PayPal replacement; P3 | Live subscriptions, merchant/button identity, configured callback, historical subscriber identifiers, replacement product/webhooks, donation/refund policy, reconciliation, Finance owner | `composer.lock`; `PayPalConfig`; `PaypalIPNController`; PayPal form views; `EmailDonorAboutUnknownPayPalPayment` |
| INT-05 | Bank statement import (`FIN-06`) | Finance-only uploaded spreadsheet parsed and matched to payment records | None visible | Supported importer; P2 | Representative redacted exports, bank/export generation, columns/encodings, duplicate detection, validation, reconciliation and Finance owner | `composer.lock`; `StatementImportController`; statement import views/tests |

## Storage, images, queues, and media

| ID | Capability / feature | Candidate-source contract | Configuration names | Approved direction / priority | Production evidence and decisions required | Source |
| --- | --- | --- | --- | --- | --- | --- |
| INT-06 | Object storage for member/equipment/expense images (`MEM-03`, `EQUIP-01`, `FIN-07`) | S3-backed public objects; environment/path conventions; resized member/equipment images; direct legacy S3 URLs; expense receipts exposed by URL | `FILE_STORE`, `AWS_KEY`, `AWS_SECRET`, `AWS_REGION`, `S3_BUCKET` | Supported Laravel filesystem; P0/P1 | Disk/bucket/region, object inventory/checksums, ACL/privacy, CORS/CDN/domain, encryption, retention/lifecycle, URL/key compatibility, migration plan, Data/Privacy and Operations owners | `config/filesystems.php`; `UserImage`; `EquipmentController`; `Equipment`; `ExpensesController`; `ExpensePresenter` |
| INT-07 | CCTV capture and event media (`ACCESS-07`) | Unauthenticated multipart camera endpoints accept single/event frames, synchronously create public JPEG/GIF objects, discard short events, and delete source frames; bucket/path assumptions are partly hard-coded | AWS/S3 names in INT-06 | Supported authenticated media replacement; P3 | Camera/firmware inventory, captured payloads, network/auth model, cadence/timezone, required GIF outcome, lawful basis, access/retention/deletion, storage volume, Physical Access/Data Privacy owners | `CCTVController`; camera routes; `intervention/image`; `sybio/gif-creator`; S3 adapter |
| INT-08 | Queue execution / optional AWS SQS | Mail/listener work implements queue interfaces; legacy configuration can select sync or SQS | `QUEUE_DRIVER`, `AWS_KEY`, `AWS_SECRET`, `AWS_QUEUE_URL`, `AWS_REGION` | Supported Laravel queue selected before cutover; operational priority follows consuming features | Production driver, worker count/process manager, retry/timeout/dead-letter behavior, payload encryption/PII, monitoring, Operations owner | `config/queue.php`; `.env.example`; queued mail/listeners |
| INT-09 | Gravatar fallback | Missing member photo can generate a Gravatar URL using an MD5 email hash | None visible | Preserve fallback outcome only if privacy-approved | Actual use, consent/lawful basis, external disclosure policy, local replacement/default image | `UserImage::getGravatarUrl()` and member-photo presentation |

## Communications, realtime, and telemetry

| ID | Capability / feature | Candidate-source contract | Configuration names | Approved direction / priority | Production evidence and decisions required | Source |
| --- | --- | --- | --- | --- | --- | --- |
| INT-10 | Transactional and broadcast email (`COMM-02`, `COMM-03`, `COMM-04`) | Queued Laravel mail for lifecycle, password/account, expense, finance, feedback, broadcasts, and PayPal donation handling; several recipients/reply-to addresses are embedded | `MAIL_DRIVER`, `MAIL_HOST`, `MAIL_PORT`, `MAIL_ENCRYPTION`, `MAIL_USERNAME`, `MAIL_PASSWORD` | Laravel Mail/Notifications; P1/P2 | Provider/domain, sender/reply-to, SPF/DKIM/DMARC, queues/retries, bounce/suppression, consent/audience audit, templates/retention, Membership/Data Privacy owners | `config/mail.php`; `UserMailer`; mail calls/listeners; email views |
| INT-11 | Discord lifecycle notifications (`COMM-05`) | `UserObserver::sendSlackNotification()` posts JSON `content` to a Discord webhook on joined/suspended/left events; old Slack package/fields remain but sends are commented | `DISCORD_WEBHOOK`; legacy `SLACK_ENDPOINT` | Supported Discord webhook; P3; Slack internals not required unless production evidence proves another outcome | Server/channel/webhook, desired events/content, retry/failure/audit behavior, secret rotation, surviving Slack workflow, Membership/Operations owners | `UserObserver`; `config/slack.php`; Slack listeners/package |
| INT-12 | Member notifications and realtime activity (`AUTH-02`, `REPORT-02`) | Pusher private member channel authorization at `POST /session/pusher`; private notification events; public activity channel includes member/activity fields; browser key is hard-coded in legacy assets | `BROADCAST_DRIVER`, `PUSHER_APP_ID`, `PUSHER_APP_KEY`, `PUSHER_APP_SECRET` | Supported Laravel broadcasting/realtime replacement; P2 | Active app/region/consumers, exact event contracts, public-channel privacy, delivery/reconnect guarantees, provider choice, Technical/Data Privacy owners | `config/broadcasting.php`; `SessionController::pusherAuth()`; broadcast events; `resources/assets/js/app.js`; realtime view |
| INT-13 | Production error reporting (`OPS-03`) | Server Rollbar provider plus embedded production browser shim/token; framework logging/Whoops also present | `ROLLBAR_TOKEN` | Supported error-reporting replacement; proposed P1 | Current project/use, environments/releases, PII scrubbing, alert routing, retention, incident owner, token rotation | `composer.lock`; `config/services.php`; `config/app.php`; `partials/rollbar-js.blade.php`; layouts |
| INT-14 | Usage analytics (`ANALYTICS-01`) | Production layouts load legacy Universal Analytics and send page views plus authenticated internal user ID | Hard-coded legacy identifier; no config name | Privacy-approved analytics replacement; proposed P2 | Current reporting use, decisions supported, consent/lawful basis, identifier policy, retention, replacement/retirement implementation, Client/Product and Data Privacy owners | `resources/views/layouts/main.blade.php`; `bare.blade.php` |

## Operations and documentation

| ID | Capability / feature | Candidate-source contract | Configuration names | Approved direction / priority | Production evidence and decisions required | Source |
| --- | --- | --- | --- | --- | --- | --- |
| INT-15 | Scheduled-job heartbeat monitoring (`OPS-02`) | Eight post-run callbacks issue GET requests to embedded Envoyer Beats URLs in production; success-only semantics are unproven | `APP_ENV`; URLs themselves act as secrets | Supported monitoring with secret/config handling; P1 | Active scheduler/heartbeats, failure semantics, missed-run thresholds, alert routing/escalation, chosen platform, Operations owner | `Console\Kernel`; `docs/parity/commands.md` |
| INT-16 | Secure log/debug access (`ADMIN-03`) | Admin `/logs` package route; Clockwork/provider/debug metadata and Whoops are present, with production enablement unconfirmed | `APP_DEBUG` and application logging configuration | Secure observability replacement; P3 | Operator use cases, log sources/volume, access/audit/redaction, retention, debug state, central platform, Operations owner | log-viewer/Clockwork/Whoops packages; log route; `config/clockwork.php`; providers |
| INT-17 | API documentation (`ADMIN-02`) | Public `/api-docs` and `/api-docs.json`; request can scan source, delete/recreate storage docs, and serve incomplete Swagger 2 annotations/UI | Swagger configuration, no secret | Generated/validated OpenAPI 3 contract and UI; P3 | Audience/auth, authoritative route/schema coverage, examples/versioning, device compatibility validation, CI generation, Technical owner | Swagger package/config; docs routes; ACS annotations; bundled UI |
| INT-18 | Hosting/deployment | Footer claims DigitalOcean/Forge; repository/GitHub contain no deployment record or tag, so this is not production evidence | Deployment secrets intentionally absent | Resolve through `NEW-001` | Hosting account/environment, deployed SHA, deploy source/script, workers/cron, TLS/DNS, backups, rollback authority | Static footer; local refs/tags; read-only GitHub deployments query on 18 August 2026 |

## Device and access contracts

| ID | Contract / features | Authentication and request | Response / side effects | Priority | Production evidence and decisions required | Source |
| --- | --- | --- | --- | --- | --- | --- |
| INT-19 | Main-door legacy endpoint (`ACCESS-02`) | Unauthenticated `POST /access-control/main-door`; `data` contains tag, optional delayed marker, or device boot/heartbeat message | Always HTTP 200; JSON uses string `valid`; logs access/system check-in and preserves exact framing expectations | P0 | Deployed clients/firmware, captured payload/response bytes, network/TLS/IP controls, offline/delayed rules, authentication rollout | route; `AccessControlController`; `KeyFobAccess`; access tests |
| INT-20 | Legacy equipment endpoint (`ACCESS-02`, `ACCESS-04`) | Unauthenticated `POST /access-control/device`; `data=tag|device|start|ping|end` | HTTP 200 JSON; opens/updates/closes equipment sessions | P0 | Devices/firmware, concurrency/offline behavior, response contract, auth rollout, session reconciliation | route; `DeviceAccessControlController`; `DeviceSession`; tests |
| INT-21 | Generic legacy ACS (`ACCESS-05`) | Unauthenticated `POST /acs`; accepts device/service/message/tag/time/payload/signature/nonce/session fields, but signature/nonce are not verified | Multiple 200/400/404 JSON shapes; system commands/status, entry/member, usage/session behaviors | P1 | Active clients/services, exact schemas/framing, queued-command consumers, clock/replay window, signature migration, compatibility period | route; `ACSController`; `ACSValidator`; ACS tests |
| INT-22 | Spark compatibility endpoint (`ACCESS-05`) | Unauthenticated `POST /acs/spark`; device/tag/service | Valid fob records fixed balance charge/activity and returns `201 OK`; invalid returns 404 | P1 | Active Spark clients, amount/reason semantics, payload/framing, replacement/compatibility window | route; `ACSSparkController` |
| INT-23 | Newer ACS node/status/activity API (`ACCESS-03`, `ACCESS-04`) | `ApiKey` header matches node record; status by tag; boot/heartbeat; activity create/update/delete with tag/device/time | JSON user/status/session payloads and node timestamps; node-to-device/action authorization is not explicit | P0 | Nodes/firmware, header casing, schemas, ownership checks, clock/delayed behavior, per-device authorization, credential rotation | ACS routes/middleware/controllers/repositories; annotations/tests |

## Completion checklist for the integration portion of NEW-003

- [x] Inventory candidate-source payment, storage/media, mail, realtime, telemetry, monitoring, documentation, hosting, queue, and device integrations.
- [x] Record source-visible endpoints, configuration-key names, payload responsibilities, approved replacement direction, and source references.
- [x] Add missing required error-reporting and analytics outcomes to the feature ledger.
- [ ] Confirm the deployed revision and reconcile live routes/configuration against provider/device dashboards.
- [ ] Capture redacted request/response fixtures for every active provider and device contract.
- [ ] Name Finance, Membership, Physical Access, Operations, Technical, and Data Privacy owners.
- [ ] Approve provider/platform selections, privacy/retention rules, secret rotation, compatibility windows, and rollback behavior.
- [ ] Reconcile provider identifiers, object keys, queued work, devices, and financial totals on production-shaped data.
